node_modules/
.env
.env.local
*.log
.DS_Store
# scratch: mockups, investigation scripts, captured data
*.tmpl.html
preview.html
mockup*.html
board.json
intel.json
mock.json
mockslim.json
snapshot.json
ponsjs.txt
batch.mjs
factory.mjs
cap.mjs
issuer.mjs

# legacy cohort dirs from the pre-outcome pipeline — ROOT-ANCHORED so they don't also ignore study/profiles (the
# committed incremental cache). An unanchored `profiles/` matches at ANY depth and silently kept the cache out of git.
/profiles/
/winners_full/
/losers/
/losers.json
data/
